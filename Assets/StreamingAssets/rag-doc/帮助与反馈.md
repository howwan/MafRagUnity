

  几点说明：

  

  - T-15 的文件名在任务描述中写的是 FeedbackForm.vue，实际实现为

  FeedbackPage.vue（功能一致且已正确接入路由），我已同步更新了任务描述。

  - T-26~T-29 的 4 个 E2E 测试都合并写在了 frontend/tests/help-feedback.spec.ts

  这一个文件中。

  - 后端没有独立的 JUnit 测试文件针对 help/feedback 功能（只有 Playwright E2E

  测试）。

---

All 16 tests pass (8 Chromium + 8 Mobile Chrome). Let me mark the task

  complete.

  

⏺ Here's a summary of everything implemented and verified:

  

  **Implementation Summary**

  **Backend (Java/Spring Boot)**

  - **HelpArticle entity** (modules/system/model/entity/HelpArticle.java) —

  help_article table with title, contentMd, category, sortOrder, soft delete

  - **UserFeedback entity** (modules/system/model/entity/UserFeedback.java) —

  user_feedback table with type, content, images (JSON via StringListConverter),

   status, reply

  - **Repositories** — HelpArticleRepository (soft-delete queries),

  UserFeedbackRepository (role-based filtering)

  - **Service layer** — HelpArticleService (CRUD + grouped list), FeedbackService

  (submit, role-filtered list, status update)

  - **Controllers** — Public GET /api/system/help, authenticated POST/GET 

  /api/feedback, admin CRUD /api/admin/help and PUT 

  /api/admin/feedback/{id}/status

  - **Security** — Public help endpoints added to SecurityConfig permitAll list

  - **Test data** — 5 seed articles across 3 categories + 1 feedback in

  TestUserInitializer

  **Frontend (Vue 3)**

  - **HelpCenter.vue** — /help route, articles grouped by category with collapsible

  sections, search filter, basic Markdown rendering

  - **FeedbackPage.vue** — /feedback route, type selector (bug/suggestion), content

  textarea, image upload placeholder, submission history timeline

  - **Services** — help.ts and feedback.ts API wrappers

  - **i18n** — zh.json and en.json updated with help/feedback keys

  editor/preview with Markdown

  - **FeedbackManage.vue** — /system/feedback route, feedback table with filters,

  detail dialog with inline status/reply editing

  - **API** **services** — help.ts and feedback.ts

  - **Routes** — Registered under system with system:manage permission

  - **Menu** — Added to sidebar under System Settings with proper icons (Reading,

  ChatDotSquare)

  - **System** **index** — Navigation cards added for Help Center and Feedback

  - **i18n** — zh-CN.ts and en.ts updated

  

  **E2E** **Tests**

  

  - **8** **tests** covering: frontend help center (load, search, expand), frontend

  feedback page (form), admin help center (load, create), admin feedback (load,

  detail)

  - **All** **16** **passes** across Chromium + Mobile Chrome

全部通过。总结一下修复的内容：

  

  **后台反馈管理截图加载不了** — 根本原因是前端图片上传只创建了本地的 blob:

  URL，没有真正存到服务器。做了三处修复：

  

  1. **后端** — MinioService 接口新增 uploadFile() 方法，FeedbackController 新增

  POST /api/feedback/upload 端点，接收 multipart 文件并上传到

  MinIO，返回可访问的 URL

  2. **前端服务** — feedback.ts 新增 uploadImage(file) 方法，上传文件到新端点

  3. **前端页面** — FeedbackPage.vue 的 handleSubmit 改为：先逐个上传文件到 MinIO

  拿到真实 URL，再提交反馈。form.files 数组跟踪原始 File 对象，上传完成后清理

  

  现在提交反馈时图片会真正上传到 MinIO 服务器，管理后台和前台都能正常加载预览。
