

 All tasks are complete. Here's a summary of everything that was implemented:

  Backend (10 new files + 2 modified)
  
  New files in modules/project:
  - model/Highlight.java — JPA entity mapped to highlight table
  - dto/HighlightDto.java, CreateHighlightDto.java, UpdateHighlightDto.java,
  SortItemDto.java — DTOs with validation
  - repository/HighlightRepository.java — JPA repository with query methods
  - service/HighlightService.java + HighlightServiceImpl.java — business logic
  - controller/HighlightController.java — REST API with 5 endpoints
  - controller/PublicProjectController.java — GET  
  /api/public/project/{id}/config with highlights
  
  Modified:
  - TestUserInitializer.java — generates 3 sample highlights
  - SecurityConfig.java — /api/public/** already permitted (no change needed)
  
  Frontend (3 files modified)
  
  - types/index.ts — added Highlight interface
  - services/project.ts — added getProjectHighlights() API call
  - components/panels/ProjectInfoPanel.vue — carousel display of highlights with
   image preview
  - locales/zh.json + en.json — added explore.highlights i18n key
  
  Admin (3 files modified)
  
  - types/project.ts — added HighlightDto, CreateHighlightDto,
  UpdateHighlightDto
  - api/project.ts — added highlightApi with CRUD + batchSort
  - views/project/edit.vue — added "亮点管理" tab with drag-sortable table,
  add/edit dialog, image upload, delete confirmation
  
  Verification
  
  - Backend builds and all APIs tested (CRUD, batch-sort, public config)
  - Frontend passes type-checking
  - Admin passes type-checking and Playwright test confirms all 4 tabs render
  correctly with highlight data visible

All E2E tests pass. Summary of what was done:

  Admin — 13/13 tests pass:
  - Fixed column header locators scoped to .highlight-table to avoid strict-mode
   ambiguity with other tables on the page 
  - Retested with --workers=1 to avoid parallelism flakiness
  
  Frontend — 13/13 tests pass:
  - Found and recreated missing index.html (was deleted from the project root)
  - Fixed backend HighlightController.java: removed
  @PreAuthorize("hasRole('USER')") from the GET 
  /api/projects/{projectId}/highlights endpoint — the URL is already permitAll()
   in SecurityConfig, but the method-level annotation was blocking public access
  - Fixed 2 tests to match real component behavior: carousel uses
  indicator-position="none" so tests now check for .el-carousel__item instead of
   .el-carousel__indicator
