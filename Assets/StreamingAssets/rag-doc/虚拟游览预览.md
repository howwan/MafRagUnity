
 **Changes Summary**

  **Backend (Java / Spring Boot)**

  - **New DTO**: PreviewTokenDto.java with token, url, expiresIn fields

  - **JwtTokenProvider**: Added generatePreviewToken(), getTourIdFromPreviewToken(),

   getProjectIdFromPreviewToken() methods

  - **VirtualTourController**: Added POST /{id}/preview endpoint returning a 5-min

  JWT token + preview URL

  - **New Controller**: PublicTourPreviewController at GET 

  /api/public/tour-preview?token=xxx for public token validation

  - **Config**: Added app.jwt.expiration-preview=300000 in application.properties

  - **Unit Test**: Updated VirtualTourControllerTest with JwtTokenProvider mock and

  preview test

  - **Integration Test**: Added preview token generation/validation tests to

  test_virtual_tours.sh

  

  **Admin (Vue 3 + Element Plus)**

  

  - **API**: Added tourApi.preview(id) in api/tour.ts

  - **Types**: Added PreviewTokenResponse interface

  - **UI**: Added green "预览" button in the tour table, widened actions column to

  300px

  - **i18n**: Added tour.preview and tour.previewFailed in zh-CN and en

  - **E2E Test**: Added "should generate preview link" test in tour.spec.ts

  

  **Frontend (Vue 3 + Three.js)**

  

  - **New Page**: TourPreview.vue — loads token from URL, validates via API, shows

  entry button

  - **Route**: Added /preview/tour with public: true meta

  - **API**: Added previewTour(token) in services/virtualTour.ts

  - **i18n**: Added tourPreview.* keys in zh.json and en.json

  - **E2E Test**: Created tour-preview.spec.ts with tests for missing/invalid/valid

  token scenarios

  

  **Verified**

  

  - Backend compiles (./gradlew compileJava ✅)

  - Admin type-checks (pnpm type-check ✅)

  - Frontend type-checks (pnpm type-check ✅)

  - Backend running and all API endpoints respond correctly

  - Preview token generation returns {token, url, expiresIn} with 300s TTL

  - Public preview validates token and returns tour data (invalid tokens

  return 401)

  - Integration tests pass for all preview scenarios
