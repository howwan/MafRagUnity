

**Summary**

  All phases completed. Here's what was implemented:

  **Backend**

  - **Project entity**: Added coreParams (JSONB) and thumbnail fields

  - **ProjectVersion entity**: New entity with id, projectId, versionNo, snapshot

  (JSONB), changeSummary, createdBy, createdAt — with UNIQUE(project_id,

  version_no)

  - **Repository**: ProjectVersionRepository with queries for listing, finding

  latest, and finding by id+project

  - **DTO/Mapper**: ProjectVersionDto, ProjectVersionMapper, and updated

  CreateProjectDto/UpdateProjectDto/ProjectDto/ProjectMapper

  - **Service**: saveVersionSnapshot, getVersions, rollback methods implemented.

  Publish auto-saves snapshot

  - **Controller**: GET /api/projects/{id}/versions and POST 

  /api/projects/{id}/rollback/{versionId}

  - **Test data**: TestUserInitializer creates projects with coreParams/thumbnail

  and version records

  **Frontend**

  - Project list (ProjectList.vue) shows thumbnail (prioritized over coverImage)

   and core_params tag chips

  - Project TypeScript interface updated with new fields

  **Admin**

  - Project edit page redesigned with two-column layout (left: tabs, right:

  version history sidebar)

  - Version history uses Element Plus Timeline, shows version tags, summaries,

  dates, and rollback buttons

  - Core parameters editor with live JSON preview tags

  - Thumbnail upload field

  - i18n keys added for zh-CN and en

  

  **E2E Tests**

  

  - **Admin**: 4 new version management tests (sidebar visibility, timeline,

  rollback dialog, new fields) — all pass

  - **Frontend**: 2 new project list tests (core params display, navigation) — all

  pass across Chromium + Mobile Chrome

  - **Regressions**: Existing tests verified — 296/296 admin tests pass (1

  pre-existing failure), 201/201 frontend tests pass



