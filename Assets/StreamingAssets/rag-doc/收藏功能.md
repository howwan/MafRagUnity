

⏺ 收藏功能已全部实现。以下是完成情况总结：

  

  **完成内容**

  

  **后端（7个新文件 + 5个修改文件）**

  

  **新增文件：**

  - UserFavorite.java — 收藏实体，映射 user_favorite 表

  - UserFavoriteRepository.java — JPA 仓库

  - FavoriteService.java / FavoriteServiceImpl.java — 服务接口和实现（含 Redis

  Set 同步）

  - FavoriteController.java — 提供 POST/GET/DELETE /api/favorites 三个端点

  - FavoriteRequest.java / FavoriteResponse.java — 请求/响应 DTO

  **修改文件：**

  - UnitDTO.java — 添加 isFavorite 字段（record 模式）

  - UnitMapper.java — 默认 isFavorite=false

  - UnitController.java — 注入 FavoriteService，在查询单元时标记收藏状态

  - ProjectDto.java — 添加 isFavorite 字段

  - ProjectController.java — 注入 FavoriteService，在查询项目时标记收藏状态

  **前端（2个新文件 + 7个修改文件）**

  

  **新增文件：**

  - src/services/favorite.ts — 收藏 API 封装

  - tests/favorites.spec.ts — Playwright E2E 测试

  **修改文件：**

  - src/types/index.ts — 添加 FavoriteItem、FavoriteRequest 类型，在 Unit 和

  Project 中添加 isFavorite

  - src/stores/user.ts — 添加 favorites

  state、isFavorited、addFavorite、removeFavorite、fetchFavorites

  - ProjectExplore.vue — 右上角星形收藏按钮

  - UnitListPanel.vue — 单元详情区星形收藏按钮

  - zh.json / en.json — 收藏相关国际化文本

  - main.ts — 登录恢复时自动加载收藏列表

  **设计文档**

  - spec-extra/favorites/requirements.md — 需求规格

  - spec-extra/favorites/design.md — 数据库、API、前端设计

  - spec-extra/favorites/tasks.md — 任务分解（全部完成）

  

  **API 验证结果**

  - POST /api/favorites — 201 Created ✓

  - GET /api/favorites — 200 OK（含目标简要信息）✓

  - DELETE /api/favorites — 204 No Content ✓

  - GET /api/projects/{id} — 返回 isFavorite ✓

  - GET /api/units/{id} — 返回 isFavorite ✓

  - GET /api/floors/{floorId}/units — 所有单元返回 isFavorite ✓

  - Redis Set favorite:user:{userId} 同步维护 ✓